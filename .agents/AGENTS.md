# Workspace Agent Rules

## Button & Typography Rules
- **Single-Line Button Text**: All button texts, chip labels, segment labels, and interactive action labels MUST remain strictly on a single line (`maxLines: 1`, `softWrap: false`, or wrapped in `FittedBox` / `TextOverflow.ellipsis`). Never allow button text or action labels to break/wrap into multiple lines.
